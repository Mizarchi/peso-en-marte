const numero = prompt('Escribe un número')
const otroNum = prompt('Escribe otro número')

const resultado = parseInt(numero) + parseInt(otroNum)
const mensaje = 'la suma de estos dos números son ' + resultado

document.write(mensaje)
